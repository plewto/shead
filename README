Shead README

Shead is a head like utility for source files.  Instead of printing the
first n-lines, it prints the initial comment block, up to the first
non-comment line.  It knows both single-line and block comment styles for
about a dozen languages. 
